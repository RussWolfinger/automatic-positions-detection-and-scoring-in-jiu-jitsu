Download mmetection model from here:

Save it in ./checkpoints/detection/   and name it deformable_detr_twostage_refine.pth

Download ViTPose model from here:

Save it in ./checkpoints/ and name it vitpose-h.pth

Use pip to install all required python packages as described in the repo.

Update your local git using the pushes I made--hacks to avoid calling Valter's tracker model which he did not include and which we don't really need anyway.

Note new video in ./inputs/craig_jones_triangle_escape.mp4

From the root directory, run   python ../../scripts/run_tracker.py